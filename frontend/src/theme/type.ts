export type Theme = {
  id: string
  name: string
  primaryColor: string
  secondaryColor: string
  primaryColorDarkMode: string
  secondaryColorDarkMode: string
  headerFont: string
  normalFont: string
}
